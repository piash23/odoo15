<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="area_view_form" model="ir.ui.view">
        <field name="name">area.form</field>
        <field name="model">area</field>
        <field name="arch" type="xml">
            <form string="Area">
                <sheet>
                    <group>
                        <group>
                            <field name="code"/>
                            <field name="name"/>
                            <field name="active"/>
                        </group>
                    </group>
                </sheet>
                <div class="oe_chatter">
                    <field name="message_follower_ids" groups="base.group_user"/>
                    <field name="activity_ids"/>
                    <field name="message_ids"/>
                </div>
            </form>
        </field>
    </record>

    <record id="area_view_tree" model="ir.ui.view">
        <field name="name">area.tree</field>
        <field name="model">area</field>
        <field name="arch" type="xml">
            <tree string="Area">
                <field name="code"/>
                <field name="name"/>
                <field name="active"/>
            </tree>
        </field>
    </record>

    <record id="area_view_search" model="ir.ui.view">
        <field name="name">area.search</field>
        <field name="model">area</field>
        <field name="arch" type="xml">
            <search>
                <field name="code"/>
                <field name="name"/>
                <filter string="Active" name="active" domain="[('active', '=', True)]"/>
                <filter string="Inactive" name="inactive" domain="[('active', '=', False)]"/>
            </search>
        </field>
    </record>

    <record id="action_area" model="ir.actions.act_window">
        <field name="name">Area</field>
        <field name="res_model">area</field>
        <field name="view_mode">tree,form</field>
        <field name="view_id" ref="xt_freight_management.area_view_tree"/>
        <field name="context">{'search_default_active': 1}</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create an Area!
            </p>
        </field>
    </record>

    <menuitem id="menu_area"
              parent="xt_freight_management.menu_others_config"
              action="action_area" sequence="1"/>
</odoo>
